# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
*.egg
build/
dist/

# Virtual environments
venv/
.venv/
env/

# Data - real Kaggle dataset is large (~150MB) and should not be
# committed; synthetic fallback is generated on the fly if missing.
data/raw/creditcard.csv

# Note: model artifacts (models/*.joblib) and the generated CSVs
# (models/test_stream.csv, data/processed/train_baseline.csv) are
# intentionally NOT ignored - they're committed so the deployed demo
# and dashboard work immediately without requiring training first.
# Re-run python -m src.models.train any time to regenerate them.

# MLflow
mlruns/
mlflow.db

# Streamlit
.streamlit/secrets.toml

# Env files
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/